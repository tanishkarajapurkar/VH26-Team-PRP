# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================
PORT=5000
HOST=0.0.0.0

# SUPABASE CREDENTIALS
# If provided, the backend reads and writes directly to your Supabase PostgreSQL DB.
# If left empty or placeholder, it automatically uses the local in-memory store.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
